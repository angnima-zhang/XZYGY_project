# 功能脚本审阅与优化建议

审阅范围：`assets/resources/scripts` 下的游戏功能脚本，不包含 `extensions/cocos-mcp-server` 编辑器扩展代码。项目版本为 Cocos Creator 3.8.7，`tsconfig.json` 当前关闭了 `strict`。

## 脚本职责梳理

| 脚本 | 主要职责 |
| --- | --- |
| `LoadingCtrl.ts` | 加载页控制器，用模拟进度条展示启动加载过程，完成后切到 `main` 场景。 |
| `MainSceneController.ts` | 主场景入口，初始化 `AudioManager`、`GameManager`、`VfxManager`，管理主页/升级页/状态页/设置弹窗切换。 |
| `ZpixFontManager.ts` | 单例字体管理器，异步加载 `resources/fonts/zpix` 并缓存 `Font`。 |
| `ZpixFontSetter.ts` | 场景字体应用器，启动后遍历当前节点树，把未指定字体的 `Label` 替换为 zpix。 |
| `utils/NumberFormatter.ts` | 数值格式化工具，处理金额、时间、百分比、次数等显示文本。 |
| `core/PlayerData.ts` | 玩家数据单例，管理余额、升级状态、统计、保底、每日重置、本地存档和调试配置。 |
| `core/GameManager.ts` | 核心玩法单例，负责抛硬币判定、暴击/连击/保底/得分、自动翻转、升级购买和翻转事件回调。 |
| `core/AudioManager.ts` | 音频单例，动态创建 BGM/音效 `AudioSource`，加载 resources 音频、播放音效、存储音频开关。 |
| `ui/MainUI.ts` | 主页 UI，监听翻转结果并刷新余额、进度、抛币次数和目标剩余金额。 |
| `ui/CoinController.ts` | 硬币交互与动画控制，处理点击、播放翻转动画、飘字、暴击/连击显示、按钮禁用恢复。 |
| `ui/UpgradePageCtrl.ts` | 升级页显示/隐藏和余额显示，处理返回按钮。 |
| `ui/UpgradeItemCtrl.ts` | 单个升级项 UI 和购买逻辑，显示当前值/下级值/价格，余额不足时置灰。 |
| `ui/StatusPageCtrl.ts` | 状态页显示/隐藏，展示预期收益、总翻转、最高连击、总暴击、自动时间。 |
| `ui/StatsPopupCtrl.ts` | 旧属性弹窗控制器，展示统计卡片和属性值，带弹窗动画。 |
| `ui/ShopPopupCtrl.ts` | 旧升级弹窗控制器，批量刷新升级项并提供自动翻转按钮，带弹窗动画。 |
| `ui/SettingPopupCtrl.ts` | 设置弹窗，控制音乐、音效、震动开关并保存到 localStorage。 |
| `ui/AutoFlipBuyCtrl.ts` | 主页自动翻转购买入口，扣除 `time` 价格后启动自动翻转。 |
| `ui/ButtonStateManager.ts` | 按钮状态管理器，翻转时禁用除设置外的按钮并缓存原状态。 |
| `ui/VfxManager.ts` | VFX 与音效统一调度，集中注册各特效节点并按玩法事件播放。 |
| `ui/VfxController.ts` | 单个 VFX 节点控制器，播放/停止/暂停/恢复动画，支持循环和定时隐藏。 |
| `ui/DebugConfig.ts` | 调试配置组件，把编辑器面板中的初始数值写入 `PlayerData` 并重置数据。 |

## 优先修复问题

### P0：`GameManager` 存在编译/运行级错误

- `GameManager.ts:177`、`229`、`231`、`234` 使用了 `_streak`、`_critCount`，但类中没有声明这些字段。应改用 `PlayerData.getCurrentStreak()`、`updateStreak()`、`incrementCritCount()`，或补齐字段并保证和存档状态一致。
- `GameManager.ts:178` 调用 `calculateScore(newStreak, isCrit)`，但函数签名是 `calculateScore(isCrit: boolean, streak: number)`。当前参数顺序反了，得分会错误。
- `GameManager.ts:180` 和 `232` 附近构造的 `FlipResult` 缺少接口要求的 `pityCount`。要么补字段，要么把接口改为可选。
- `GameManager.ts:203` 的 `flipCoin(): FlipResult` 在多处用 `return null as any`。建议返回类型改为 `FlipResult | null`，并让调用方显式处理失败。
- `GameManager.ts:513` 的自动翻转直接调用 `flipCoin()`，但新流程需要先 `prepareFlip()`。这会走 legacy 随机逻辑，和动画预定结果机制不一致。

### P0：设置弹窗的事件解绑引用了不存在字段

- `SettingPopupCtrl.ts:163`、`169`、`175` 引用了 `this.musicOffNode`、`this.soundOffNode`、`this.vibrateOffNode`，类中没有这些属性。应在 `onLoad` 中缓存 on/off 子节点为成员，或在 `onDestroy` 里重新 `getChildByName()` 后逐个解绑。
- `SettingPopupCtrl.ts:107` 加载 localStorage 后，`onEnable` 又先从 `AudioManager` 取值、再 `loadSettings()` 覆盖，可能导致两个存储源状态互相覆盖。建议只保留一个设置源，音频开关以 `AudioManager` 为唯一状态更稳。

### P0：多个 `onFlip` 监听无法正确解绑

- `CoinController.ts:182` 注册时使用 `this.onFlipResult.bind(this)`，`CoinController.ts:219` 解绑时又创建了一个新的 bind 函数，`offFlip` 找不到原引用，会导致组件销毁后仍收到回调。
- `MainUI.ts:80` 也用 bind 注册，但 `onDestroy` 没有实际解绑。建议把绑定函数保存成成员，例如 `_onFlipResult = this.onFlipResult.bind(this)`，注册和注销都使用同一个引用。

### P0：`StatsPopupCtrl` 使用了已不存在的升级类型

- `StatsPopupCtrl.ts:278`、`284`、`290`、`296`、`314` 调用 `getUpgradeValue('animSpeed'/'headProb'/'critRate'/'critBonus'/'autoDuration')`，但 `UpgradeType` 实际是 `speed/lucky/critical/criticalBonus/time`。如果这个脚本仍挂在场景里，会直接类型报错或运行异常。

## 中高优先级优化

### 数据与规则

- `PlayerData.ts:576` 的每日重置会清空 `stats`，但字段名是 CareerStats，注释也写“生涯统计”。如果设计上是每日统计，应改名；如果是生涯统计，不应每日清空。
- `PlayerData.ts:308` 对 `pity` 使用 `currentValue - 1`，没有下限保护。升到 0 后保底逻辑会变成每次都触发，建议 `Math.max(1, currentValue - 1)`。
- `PlayerData.ts:387` 的 `applyDebugConfig()` 会在 `DebugConfig.onLoad()` 中重置玩家数据。确认只在调试场景使用，避免线上每次启动清档。
- `calculateExpectedValue()` 的公式和实际 `calculateScore()` 逻辑不完全一致，尤其连击期望、暴击和正面概率的关系偏粗略。建议把预期收益标成“估算”，或抽出一个可测试的数学模型。

### UI 与事件

- `MainUI.ts:126` 翻转后没有调用 `updateNeedAmount()`，剩余目标金额只在初次刷新时更新。建议在每次余额变化后同步刷新。
- `UpgradeItemCtrl.ts:219` 的 `refreshUI()` 是 private，但 `ShopPopupCtrl.ts:145` 用 `(ctrl as any).refreshUI()` 绕过类型系统。应改为 public，或通过事件/页面刷新接口统一刷新。
- `UpgradeItemCtrl.ts:235` 定义了 `updateTitle()` 但未调用，标题可能不会从 `PlayerData` 配置刷新。
- `ButtonStateManager.ts` 和 `CoinController.ts` 都实现了按钮禁用/恢复逻辑，职责重复。建议保留一个集中入口，避免两个系统同时改按钮颜色和 interactable。

### 动画、资源和生命周期

- `CoinController.ts:336` 动态修改 `AnimationClip` 私有字段 `_channel/_curve/_values`，这依赖引擎内部实现，Cocos 版本变化时风险很高。更推荐准备正反面两套动画资源，或在动画事件/结束回调中切换最终 SpriteFrame。
- `CoinController.ts:442` 每次正面都会逐帧 `resources.load` 60 张图，且没有显式缓存和释放。建议预加载或由资源配置引用，避免翻转时卡顿和重复加载。
- `LoadingCtrl.ts:128` 和 `141` 使用 `setInterval/setTimeout`，销毁时没有清理。若加载页被提前切走，回调仍可能访问已销毁节点。建议保存 timer id，并在 `onDestroy` 清理。
- `VfxController.ts:133` 监听动画结束时每次 `play()` 都创建匿名回调，虽然完成后会 off，但重复播放/提前 stop 时可能残留。建议把 finished handler 作为成员方法统一注册/注销。
- `AudioManager.ts` 动态创建的音频节点没有挂到场景或持久根节点上，虽然 Cocos 组件可在未挂树节点上工作一部分逻辑，但生命周期不清晰。建议挂到 Canvas 或使用 `director.addPersistRootNode()` 管理。
- `AudioManager.ts` 和 `ZpixFontManager.ts` 通过 `resources.load` 缓存资源，但没有 addRef/decRef 或 release 策略。长期运行问题不大，但若场景频繁切换，应明确资源生命周期。

### 类型与可维护性

- `tsconfig.json:7` 关闭了 `strict`，掩盖了大量类型问题。建议先开启 `noImplicitAny`、`strictNullChecks` 的局部修复，最后再全量 strict。
- `ShopPopupCtrl.ts`、`StatsPopupCtrl.ts` 看起来是旧弹窗体系，而当前主流程更像 `UpgradePageCtrl/StatusPageCtrl`。建议确认是否仍使用；不用就从场景和脚本引用中移除，减少旧字段名继续误导。
- 多个脚本里存在大量调试 `console.log`。建议加一个 debug 开关，或封装日志工具，避免真机/小游戏环境输出过多影响性能和可读性。
- 数字格式化逻辑分散在 `NumberFormatter`、`MainUI`、`CoinController`、`StatsPopupCtrl`。建议统一使用 `NumberFormatter`，避免同一金额在不同界面显示风格不一致。

## 建议修复顺序

1. 先修 `GameManager` 的字段、返回类型、得分参数、自动翻转流程，这是核心玩法正确性的基础。
2. 修所有事件解绑：`CoinController`、`MainUI`、`SettingPopupCtrl`，避免销毁后回调和重复监听。
3. 清理旧字段名：`StatsPopupCtrl`、`ShopPopupCtrl`，确认旧弹窗是否保留。
4. 收敛资源和动画：硬币动画不要改私有曲线，正面帧做预加载或资源引用。
5. 最后统一格式化、日志、按钮状态管理，并逐步打开 TypeScript 严格检查。

## 验证情况

尝试执行 `npx tsc --noEmit --pretty false`，但当前项目没有安装 TypeScript，`npx` 命中了错误的 `tsc@2.0.4` 占位包并退出。因此本次没有完成编译验证。建议给项目补充本地 `typescript` 依赖或使用 Cocos Creator 自带编译检查后再做一次完整验证。
